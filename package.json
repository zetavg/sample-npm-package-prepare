{
  "name": "sample-npm-package-prepare",
  "version": "1.0.0",
  "description": "A sample npm package that needs prepare (compile)",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib/*"
  ],
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3"
  }
}
