{
  "name": "sample-npm-package-prepack",
  "version": "1.0.0",
  "description": "A sample npm package that needs prepack (compile)",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "prepack": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "lib/*"
  ],
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3"
  }
}
